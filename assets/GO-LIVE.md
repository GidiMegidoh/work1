# ✅ Veltrum (טננט `shibutz`) — צ'קליסט עלייה לאוויר

> ## 🛑 סטטוס: לא מאומת מול מספר חי
> שכבת הפרודקשן (WhatsApp Cloud API, Google Calendar, דשבורד) **עדיין לא קיימת
> במוצר** — החלטת הבעלים (2026-08-05): נשארים במצב `memory`, החיווט האמיתי
> ייבנה במשימת המשך. אף סעיף בצ'קליסט הזה לא סומן, כי אף אחד מהם לא הורץ
> מול המספר החי. אסור לסמן סעיף שלא הורץ בפועל.

## מה כן אומת (סימולטור, 2026-08-05)

- `tenants/shibutz.json` עבר ולידציה: 0 שגיאות, 0 אזהרות
- כל 6 בדיקות הקבלה של תהליך ההטמעה עברו ב-`npm run sim shibutz`:
  מחיר + הסתייגות · הצעת מועדים (א׳–ה׳ בלבד, בלי שישי/שבת) · קביעה→העברה→ביטול ·
  שאלה לא מוכרת → הסלמה · טריגרים ("הנחה", "הצעת מחיר") → נציג + התראה ·
  `נציג` → העברה · `הסר` → הפסקת הודעות

---

## ⚠️ אזהרות — לקרוא לפני כל צעד בלתי הפיך

1. **המספר לא יכול להיות פעיל באפליקציה הרגילה.** מספר שמחובר ל-WhatsApp
   Cloud API חייב להיות מנותק מאפליקציית WhatsApp או WhatsApp Business
   שבטלפון. אם המספר `972524608284` פעיל היום באפליקציה — חיבורו ל-API
   ינתק אותו ממנה, וההיסטוריה לא עוברת. לשקול מספר ייעודי חדש לסוכן.
2. **אישור שם התצוגה של Meta לוקח 1–2 שבועות** ואינו בשליטת אף אחד.
   מתחילים את התהליך ביום ההחלטה, לא ביום ההשקה. שם התצוגה חייב להיראות
   כמו שם העסק האמיתי ("Veltrum") — לא סלוגן שיווקי, אחרת יידחה.

## הכנת WhatsApp (סעיף 5 בתהליך) — מוכן לביצוע כשתחליט

- [ ] הוספת המספר ב-WhatsApp Manager (business.facebook.com → WhatsApp Manager)
- [ ] הגשת שם תצוגה: `Veltrum` (לאישור Meta — ראה אזהרה 2)
- [ ] העתקת ה-**phoneNumberId** מ-WhatsApp Manager אל
      `tenants/shibutz.json` → `whatsapp.phoneNumberId` (כרגע `null`)
- [ ] הגשת תבנית Utility בשם `lead_alert` — גוף: `פנייה חדשה: {{1}}`
- [ ] הגשת תבנית Utility בשם `followup_he`

## צ'קליסט העלייה לאוויר (סעיף 6) — להריץ מול המספר החי בלבד

להדפיס, להריץ שורה-שורה מול המספר החי, לסמן ולחתום:

- [ ] הודעה מהטלפון שלך למספר החי — מתקבלת תשובה
- [ ] קביעת תור אמיתי (שיחת דמו) — נוחת ביומן *שלך* עם המשך הנכון (15 דק')
      ושם הלקוח
- [ ] כתיבת `נציג` — העברה לנציג מאושרת, והבעלים מקבל את ההתראה
      (SMS אל 972524608284)
- [ ] כתיבת `הסר` — ההודעות נפסקות
- [ ] בדיקת גדרות וורטיקל על המספר החי — לוורטיקל `general` של Veltrum:
      "אפשר הנחה?" ו"אני רוצה הצעת מחיר" חייבים להסלים לנציג, לא להיענות
- [ ] הבעלים מצליח להתחבר לדשבורד ולהשתמש ב-*קח שליטה*

> לשמור את הצ'קליסט החתום — הוא אישור העלייה לאוויר והראיה אם לקוח יטען
> בעתיד שהסוכן אמר משהו שהוא לא אומר.

## חסמים ידועים לביצוע הצ'קליסט (נכון ל-2026-08-05)

| שורה בצ'קליסט | מה חסר במוצר |
|---|---|
| הודעה למספר חי / תשובה | אין חיבור WhatsApp Cloud API (webhook + שליחה) |
| תור נוחת ביומן שלך | אין ספק יומן מלבד `memory`; אין Google Calendar |
| התראת SMS לבעלים | אין שולח SMS/תבניות; `lead_alert` לא מחווט |
| דשבורד + קח שליטה | אין דשבורד במוצר |
| ריענון טננטים (`docker compose kill -s HUP shibutz`) | אין `docker-compose.yml` בריפו — לא רלוונטי עד שיש שירות רץ |

כשהחיווט ייבנה — הצ'קליסט הזה נשאר הנוסח המחייב; רק אז מסמנים.
