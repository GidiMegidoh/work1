# 📡 אור-דנט (טננט `demo-dental`) — עלייה לאוויר על WhatsApp Cloud API

> ## 🛑 סטטוס: לא הורץ מול מספר חי
> החיווט קיים ונבדק במלואו **מקומית בלבד** (`src/whatsapp/`, טרנספורט `sim`,
> אפס קריאות רשת). כל מה שנשאר הוא הצעדים הידניים בקונסולה של Meta — הם
> למטה, לפי הסדר. אסור לסמן סעיף שלא הורץ בפועל מול המספר החי.

## מה כבר אומת (מקומית, בלי Meta)

- `tenants/demo-dental.json` — ולידציה: 0 שגיאות, 0 אזהרות
- `node src/whatsapp/selftest.js` — 49 בדיקות עברו, 0 קריאות רשת:
  אימות `hub.challenge` · טקסט נכנס → רשימה · לחיצת כפתור → המשך זרימה ·
  `נציג` → תבנית `lead_alert` אל `notifyPhones` · חתימה תקינה/פגומה ·
  משלוח חוזר של אותו `wamid` → שליחה אחת · מגבלות אורך וכמות של Meta ·
  מטענים פגומים · הודעה של טננט אחר · כשל מסירה זמני ומשלוח חוזר ·
  מונה שליחות לפי שיחה (נספר = נמסר) · שימור דירוג ליד חם בשאלה המשולבת
- `npm test` — 16/16 בדיקות + שומר הרשת, ירוק

> ✅ ששת התרחישים של `demo-dental` מכוסים ב-CI: `test/probes-demo-dental.js`
> נטען מתוך `test/run-probes.js` ורץ תחת `npm test` (בדיקות ד1–ד6), יחד עם
> שלוש בדיקות רגרסיה להתנגשויות מילות-המפתח שתוקנו (ד7–ד9, שני הכיוונים).
> עריכה עתידית של הטננט או הליבה ששוברת תרחיש — מפילה את הבדיקה.

> 🔓 `whatsapp.config.json` **אינו** ב-`.gitignore` — במכוון. אין בו סודות:
> `WA_TOKEN` / `WA_VERIFY_TOKEN` / `WA_APP_SECRET` נקראים ממשתני סביבה בלבד
> ו-`config.js` דורס ערכים כאלה אם הוכנסו לקובץ. לא לשים בו סודות.

---

## ⚠️ שתי אזהרות — לקרוא לפני הצעד הראשון

### 1. ברגע שהמספר חי — **כל** הודעה אליו מקבלת את הבוט

אין השקה חלקית, אין "רק אנשי הקשר שלי", אין מצב בדיקה על מספר חי. מהרגע
שה-webhook מחובר, כל אדם שכותב למספר הזה — לקוח, ספק, חבר, מישהו שטעה
במספר — מקבל את הסוכן. אין דרך לסנן לפי שולח לפני שההודעה מגיעה.
לכן: או מספר ייעודי חדש לסוכן, או השלמה עם זה שהמספר כולו הופך לערוץ בוט.

### 2. `notifyPhones` חייב לעבור לטלפון אחר **לפני** העלייה לאוויר

כרגע `tenants/demo-dental.json` מגדיר:

```json
"escalation": { "notifyPhones": ["972524608284"] }
```

וזה **אותו מספר שמיועד לעלות לאוויר**. אם לא משנים אותו, כל הסלמה תגרום
לבוט לשלוח התראה למספר של עצמו — הודעה עצמית. בפועל: או שההתראה לא תגיע
לאף אחד, או שהיא תיכנס בחזרה ל-webhook כהודעה נכנסת. בשני המקרים הבעלים
לא מקבל התראה, וזו בדיוק הפונקציה שהוא משלם עליה.

**לשנות ל-מספר של הבעלים/המזכירה שאיננו מספר הבוט, ורק אז להמשיך.**

---

## הצעדים הידניים ב-Meta — לפי הסדר

### 1. ניתוק המספר מאפליקציית WhatsApp
מספר שמחובר ל-Cloud API לא יכול להיות פעיל במקביל באפליקציית WhatsApp או
WhatsApp Business שבטלפון. אם המספר פעיל היום באפליקציה — למחוק שם את
החשבון (הגדרות → חשבון → מחיקת חשבון) **לפני** ההוספה ב-WhatsApp Manager.
ההיסטוריה והצ'אטים לא עוברים ואי אפשר לשחזר אותם.

### 2. הוספת המספר ב-WhatsApp Manager
`business.facebook.com` → WhatsApp Manager → Phone numbers → Add phone number.
אימות ב-SMS או בשיחה קולית (צריך גישה למספר באותו רגע).

### 3. שם תצוגה — הגשה לאישור Meta
שם התצוגה: `אור-דנט` (או `Or-Dent`). חייב להיראות כמו שם העסק האמיתי —
סלוגן שיווקי נדחה. **האישור לוקח 1–2 שבועות ואינו בשליטתנו**, אז מגישים
ביום ההחלטה ולא ביום ההשקה.

### 4. `phoneNumberId` וטוקן → לקונפיג
מעתיקים מ-WhatsApp Manager את ה-**Phone number ID** (מזהה מספרי, *לא* מספר
הטלפון עצמו):

```bash
cp whatsapp.config.example.json whatsapp.config.json
```

וממלאים ב-`whatsapp.config.json` את `graphHost` (`graph.facebook.com`),
`graphVersion` ו-`phoneNumberId`. הסודות **לא** נכנסים לקובץ הזה אלא
לסביבה בלבד:

```bash
export WA_TOKEN=…          # טוקן גישה
export WA_VERIFY_TOKEN=…   # מחרוזת שאתם ממציאים — תשמש בצעד 5
export WA_APP_SECRET=…     # Meta App Dashboard → Settings → Basic → App Secret
```

> ⏳ **הטוקן הזמני שמופיע בקונסולה תקף ל-24 שעות בלבד.** אם משתמשים בו,
> הדמו מת למחרת באמצע השבוע. חייבים טוקן קבוע של **System User**:
> Business Settings → Users → System Users → יצירת משתמש → Generate token
> עם ההרשאות `whatsapp_business_messaging` ו-`whatsapp_business_management`,
> בלי תאריך תפוגה. את הטוקן הזה שמים ב-`WA_TOKEN`.

בדיקה שהקונפיג נקלט:

```bash
node src/whatsapp/server.js demo-dental
```

ואז `http://localhost:3100/health` — `missingForLive` צריך לחזור ריק.

### 5. חיווט ה-webhook — בלי זה שום הודעה לא מגיעה

זה הצעד שהופך את המספר מ"קיים" ל"עונה". בלעדיו הכול מוגדר ודומם.

1. להרים את `src/whatsapp/server.js` מאחורי **כתובת HTTPS ציבורית**.
   Meta לא קוראת ל-`localhost` ולא מקבלת תעודה עצמית — צריך דומיין עם
   תעודה תקפה, או מנהרה (`ngrok http 3100` וכדומה) לצורך הבדיקה הראשונית.
2. Meta App Dashboard → **WhatsApp → Configuration → Webhooks → Edit**:
   - **Callback URL**: `https://<הדומיין-שלכם>/webhook`
   - **Verify token**: בדיוק אותה מחרוזת שהוגדרה ב-`WA_VERIFY_TOKEN` בצעד 4
3. ללחוץ **Verify and save**. Meta שולחת מיד `GET` עם `hub.challenge`;
   השרת חייב להחזיר 200 עם ה-challenge. בלוג של השרת יופיע `GET /webhook → 200`.
   אם חוזר 403 — הטוקנים לא זהים. אם יש timeout — הכתובת לא באמת ציבורית.
4. באותו מסך, תחת **Webhook fields**, לעשות **Subscribe** לשדה **`messages`**.
   זה השדה היחיד שהסוכן צורך; בלי מנוי אליו ה-webhook מאומת אבל שקט לחלוטין.
5. לוודא שה-WABA מנוי לאפליקציה (WhatsApp Manager → Account tools → Webhooks).

### 6. הגשת תבניות Utility
התראות לצוות הן הודעות ביוזמת העסק, ולכן חייבות תבנית מאושרת — טקסט חופשי
אל מספר שלא כתב לנו ב-24 השעות האחרונות פשוט לא יישלח.

- [ ] תבנית `lead_alert` · קטגוריה **Utility** · שפה **עברית** ·
      גוף: `פנייה חדשה: {{1}}`
- [ ] תבנית `followup_he` · קטגוריה **Utility** · שפה **עברית**

שמות התבניות מוגדרים ב-`whatsapp.config.json` תחת `templates` — אם בחרתם
שמות אחרים, לעדכן שם.

### 7. ריענון
לטעון מחדש את השירות כדי שיקרא את הקונפיג והטננט המעודכנים:

```bash
WA_MODE=live node src/whatsapp/server.js demo-dental
```

לוודא בלוג: `מצב: live` ו-`חסר לעלייה לאוויר` לא מופיע.

---

## ✅ צ'קליסט העלייה לאוויר — להריץ מול המספר החי בלבד

להדפיס, להריץ שורה-שורה, לסמן ולחתום:

- [ ] `notifyPhones` שונה למספר שאינו מספר הבוט (אזהרה 2) — **חוסם**
- [ ] הודעה מטלפון פרטי למספר החי — מתקבלת תשובה תוך שניות
- [ ] `כמה עולה סתימה?` — מחזיר `350–450 ₪` **וגם** את הצהרת המחירים
- [ ] `אני רוצה לקבוע תור לניקוי` — מוצגת רשימת מועדים אמיתית,
      בלי שישי אחרי 13:00 ובלי שבת
- [ ] קביעת תור מלאה → שינוי מועד → ביטול, עם אותו מספר אסמכתא
- [ ] `כואבת לי שן, מה כדאי לקחת?` — סירוב לייעוץ רפואי + הצעת מועדים
- [ ] `נציג` — הלקוח מקבל אישור **והבעלים מקבל את `lead_alert`** במספר החדש
- [ ] `הסר` — ההודעות נפסקות
- [ ] שליחת הודעה מהמספר החי לעצמו **אינה** יוצרת לולאה
- [ ] הרצת webhook חוזר (Meta שולחת שוב) אינה יוצרת תשובה כפולה

> לשמור את הצ'קליסט החתום — הוא אישור העלייה לאוויר והראיה אם לקוח יטען
> בעתיד שהסוכן אמר משהו שהוא לא אומר.

---

## 💸 עלויות תפעול — חיוב פר-הודעה מ-01.10.2026

> מ-**01.10.2026** Meta מחייבת **כל הודעה בנפרד** — גם תשובות שירות בתוך
> חלון 24 השעות **וגם תבניות Utility בתוך החלון** (כולל `lead_alert`) —
> לפי תעריף Utility לישראל. התעריף המדויק יפורסם עד 01.09.2026.

הודעות-לשיחה, נמדד בפועל (`node src/whatsapp/measure-scenarios.js`,
טרנספורט sim, אפס רשת). "לפני" = לפני איחוד ההודעות; "אחרי" = המצב הנוכחי:

| תרחיש | לפני | אחרי |
|---|---|---|
| 1. שאלת מחיר + הצהרה | 1 | 1 |
| 2. הצעת מועדים בשעות הפעילות | 1 | 1 |
| 3. קביעה → העברה → ביטול | 11 | **10** |
| 4. שאלה מחוץ ל-FAQ → הסלמה (תשובה + תבנית) | 2 | 2 |
| 5. גדר רפואית: סירוב + הצעת תור | 2 | 2 |
| 6. נציג → העברה + התראה (תשובה + תבנית) | 2 | 2 |
| **סה"כ ששת התרחישים** | **19** | **18** |
| ייחוס: הזמנה מלאה | 6 | **5** |
| ייחוס: הזמנה שנפתחת ב"שלום" | 7 | **6** |

שני האיחודים שמאחורי ההפרש:

1. **שאלת איכות משולבת אחת במקום שתיים** — `tenants/demo-dental.json` מכיל
   שתי רשומות `qualification` עם אותו `id`: הראשונה נשאלת (שאלה משולבת),
   והשנייה לא נשאלת אבל מדרגת את אותה תשובה מול רשימת הכאב במשקל 2.
   כך דירוג הליד החם נשמר בדיוק — מאומת ב-selftest (תשובה עם «כואב» → ליד
   חם; «בדיקה שגרתית» → לא-חם).
2. **כתובת + חניה + הכנה מקופלות לאישור «התור נקבע»** — הלקוח לא צריך לשאול
   «איך מגיעים?» אחרי הקביעה; שדה `extraInfo.address` ריק לא מוסיף שורה.

המונה: `src/whatsapp/meter.js`, מחווט ב-`handler.js` ונספר **רק אחרי מסירה
מוצלחת** (נספר = נמסר = מה ש-Meta מחייבת). סיכום מודפס בסוף כל ריצת selftest.

---

## 🐛 מגבלות ידועות

שלוש התנגשויות מילות-המפתח שתועדו כאן — «אתם עובדים עם כללית?» → שעות
פעילות; «יש דמי ביטול?» → זרימת ביטול; «מאחר» בבחירת מועד → עמוד מועדים —
**תוקנו בליבה** (`KW.HOURS_EXCLUDE` · `ביטול`/`דמי ביטול` ב-`KW.POLICY` ·
`KW.MORE_EXACT`) ומכוסות ברגרסיה דו-כיוונית ב-`test/probes-demo-dental.js`
(בדיקות ד7–ד9) תחת `npm test`.

מה שנשאר מתועד:

### דפדוף מועדים: צורות «אחר» עם קידומת אינן מדפדפות עוד

תיקון «מאחר» החליף את `'אחר'` ב-`KW.MORE` בהתאמת טוקן מדויקת
(`KW.MORE_EXACT`: `אחר` / `אחרת` / `אחרים` / `אחרות`, בלי קילוף קידומות).
לכן צורות עם קידומת — «לאחר», «ואחר», «האחר» — שבעבר דפדפו את עמוד
המועדים בטעות, כבר אינן מדפדפות והן נופלות ל-FAQ או לחזרה על השאלה.
«מועד אחר», «זמנים אחרים», «אפשרות אחרת» ו-«עוד» ממשיכים לדפדף (מאומת ב-ד9).

### שתי רשומות `qualification` עם אותו `id` — בכוונה, לא באג

בלוק ה-`qualification` של `demo-dental` מכיל **בכוונה** שתי רשומות עם
`id: "qual"`: לולאת השאלות שואלת רק פעם אחת (כשמגיעים לרשומה השנייה התשובה
כבר שמורה), ו-`leadScore` מדרג את אותה תשובה שמורה מול **שתי** הרשימות —
הסיבה במשקל 1 והכאב במשקל 2. "לתקן" את הכפילות ב-port עתידי משמעו להרוג
בשקט את דירוג הליד החם בשאלה המשולבת.

---

## מה עדיין לא קיים במוצר

| יכולת | מצב |
|---|---|
| יומן אמיתי (Google Calendar) | אין — `calendar.provider` הוא `memory`, התורים נמחקים עם הפעלה מחדש |
| התמדה של שיחות ותורים | אין — הכול בזיכרון התהליך |
| דשבורד לבעלים / "קח שליטה" | אין |
| תבנית `followup_he` מחווטת לשליחה | התבנית מוגשת, אבל שום קוד עדיין לא שולח אותה |
| ניטור והתראות תפעוליות | אין |

`memory` פירושו שהפעלה מחדש של השרת מוחקת את כל התורים. לדמו זה מספיק;
ללקוח משלם — לא. זה החסם הראשון לטפל בו אחרי שהמספר עולה.
